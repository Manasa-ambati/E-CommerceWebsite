# Frontend environment variables for Railway deployment ONLY
# DO NOT deploy to Vercel - everything runs on Railway

# Backend API URL - Railway backend service
REACT_APP_API_URL=https://splendid-hope-production-fb0d.up.railway.app

# Disable ESLint errors during build
CI=false